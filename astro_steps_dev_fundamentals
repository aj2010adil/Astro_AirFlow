#Astro Project Development Steps
astro dev init -- this will create the reprository
astro dev start -- it will download all the necessary files regarding docker and astro files

#Now we have to install airflow but prior to that pip should be installed
sudo apt install python3-pip
sudo pip install apache-airflow
AIRFLOW_HOME=/opt/airflow
sudo airflow db init
## Now you have did Installation process now we can start with develoment process

#Creating DAG

